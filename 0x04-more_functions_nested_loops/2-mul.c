#include "main.h"

/**
* mul - multiplies two integars
* @c: the first number to multiply
* @d: the second number to multiply with first number
* return: the result of multiplication between c param and y param
*/

int mul(int c, int d)
{
	return (c * d);
}
