#include <stdio.h>
/**
* main - starting point of another code
* Return: 0 because i am saying so
*/
int main(void)
{
char n;
int p;

n = 'a';
p = 0;
while

(p < 10)
{
putchar(p + '0');
p++;
}
while
(n <= 'f')
{
putchar(n);
n++;
}

putchar('\n');
return (0);
}
