#include <stdio.h>
/**
* main - starting point still coding on sunday
* Return: 0 because it should be so
*/
int main(void)
{
char m;

m = 'z';
while

(m >= 'a')
{
putchar(m);
m--;
}

putchar('\n');
return (0);
}
